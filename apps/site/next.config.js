/** @type {import('next').NextConfig} */
const nextConfig = {
   experimental: {
     },
   // image optimization
   images: {
      domains: ['placehold.co', 'via.placeholder.com'],
   },
}

module.exports = nextConfig
